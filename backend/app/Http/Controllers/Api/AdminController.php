<?php

namespace App\Http\Controllers\Api;

use App\Http\Controllers\Controller;

/**
 * @deprecated Ce controller a été découpé en controllers spécialisés :
 * - AdminUserController       (gestion utilisateurs)
 * - AdminPrestataireController (gestion prestataires)
 * - AdminBudgetController     (gestion budgets)
 * - AdminAuditController       (audit logs)
 *
 * Ce fichier est conservé uniquement pour compatibilité et ne contient aucune logique.
 */
class AdminController extends Controller
{
    // Classe vide intentionnellement — toute la logique est dans les controllers spécialisés.
}