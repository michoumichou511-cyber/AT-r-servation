# Healthcheck statique : aucun bootstrap Laravel (diagnostique si /up ou /api/health échouent).
# startCommand explicite : évite un « Custom Start Command » vide ou erroné dans le dashboard.
[deploy]
startCommand = "/usr/local/bin/railway-entrypoint.sh"
healthcheckPath = "/railway-health.php"
healthcheckTimeout = 300
